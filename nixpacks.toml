[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@10.4.1", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build", "pnpm exec drizzle-kit migrate --config=drizzle.config.ts"]

[start]
cmd = "pnpm run start"
